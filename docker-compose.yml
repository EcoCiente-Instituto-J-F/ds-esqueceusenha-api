services:

  senha:
    build:
      context: .
      dockerfile: Dockerfile

    image: senha:1.0.0
    container_name: senha

    ports:
      - "${APP_PORT:-8080}:8080"

    environment:
      SERVER_PORT: "8080"

    restart: unless-stopped
